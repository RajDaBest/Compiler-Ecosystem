# arctan(x) = sum from r = 0 to infinity of ((-1) ^ r) (x ^ (2r + 1)) / (2r + 1)

	fpush 1.0 # 0
	upush 9 # 1

	spush -1 # 2

calc:	
	
	spush -1 # 3
	smult 2
	upush 1 # 4
	uminus 1
	adup 1
	jmp_if calc
	halt